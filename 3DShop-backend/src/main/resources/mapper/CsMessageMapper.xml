<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.majun.backend.repository.CsMessageRepository">

    <select id="findByConversationIdOrderByCreateTimeAsc" resultType="org.majun.backend.entity.CsMessage">
        SELECT * FROM cs_message
        WHERE conversation_id = #{conversationId}
        ORDER BY create_time ASC
    </select>

    <select id="countUnreadByConversationIdAndSenderRole" resultType="int">
        SELECT COUNT(*) FROM cs_message
        WHERE conversation_id = #{conversationId}
        AND sender_role = #{senderRole}
        AND is_read = 0
    </select>

</mapper>
